**Required to run**\
Model can be run most easily from batch file; the changes in crop, weather and soil input files and in the rotation and batch run files can be done with a text editor. To change the model routines itself, a FORTRAN editor is needed. 

**Required to develop**\
FORTRAN compiler

**Database I/O**\
text files